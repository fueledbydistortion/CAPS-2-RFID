# Semaphore SMS API Configuration
SEMAPHORE_API_KEY=your-semaphore-api-key-here
SEMAPHORE_SENDER_NAME=SmartChildcare

# Firebase Configuration (server-side - DO NOT commit real private keys)
# Set these in Vercel (or in a local .env) for the backend to initialize Firebase Admin.

# Project ID
FIREBASE_PROJECT_ID=smartchild-2e350

# Service account client email (example format)
# e.g. service-account@smartchild-2e350.iam.gserviceaccount.com
FIREBASE_CLIENT_EMAIL=your-service-account@smartchild-2e350.iam.gserviceaccount.com

# Private key (multi-line). For Vercel paste the private_key value from your service account
# JSON exactly as it appears (with \n sequences) or use a base64 encoded JSON approach.
# Example placeholder (DO NOT USE A REAL KEY HERE):
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Realtime DB and Storage bucket (defaults provided by the project)
FIREBASE_DATABASE_URL=https://smartchild-2e350-default-rtdb.firebaseio.com
FIREBASE_STORAGE_BUCKET=smartchild-2e350.firebasestorage.app

# Node environment
NODE_ENV=development
